import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-element";

<Meta
  title="Components/Button"
  argTypes={{
    label: {
      description: "Button Label",
      table: {
        type: {
          summary: "String",
        },
      },
      control: {
        type: "text",
      },
    },
    disabled: {
      description: "Disables the button",
      table: {
        type: {
          summary: "Boolean",
        },
      },
      control: {
        type: "boolean",
      },
    },
    onClick: {
      name: "click",
      action: "clicked",
    },
  }}
/>

export const Template = (args) =>
  html`<saas-button
    label="${args.label}"
    @click=${args.onClick}
    ?disabled="${args.disabled}"
    >${args.innerHtml}</saas-button
  >`;

# Button

The button component indicates the user a CTA (call to action).

Use it where you think a user should perform an action to progress throughout the application.
Do not use more than 3 buttons at the same time on one user interface. This leads to confusion for the users.

## Example

<Canvas>
  <Story name="Default" args={{ label: "Test", disabled: false }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Interfaces

Here you can see the different properties you can configure the button.

<ArgsTable story="Default" />
