import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-element";

<Meta
  title="Components/Button"
  argTypes={{
    label: {
      description: "Button Label",
      table: {
        type: {
          summary: "String",
        },
      },
      control: {
        type: "text",
      },
    },
    innerHtml: {
      description: "Inner HTML rendering in slot",
      table: {
        type: {
          summary: "String",
        },
      },
      control: {
        type: "text",
      },
    },
    onClick: {
      name: "click",
      action: "clicked",
    },
  }}
/>

export const Template = (args) =>
  html`<saas-button label="${args.label}" @click=${args.onClick}
    >${args.innerHtml}</saas-button
  >`;

# Button component

<Canvas>
  <Story name="Default" args={{ label: "Test" }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
