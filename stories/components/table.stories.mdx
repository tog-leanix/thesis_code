import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-element";

<Meta
  title="Components/Table"
  argTypes={{
    data: {
      description: "Data",
      table: {
        type: {
          summary: "Array",
        },
      },
      control: {
        type: "array",
      },
    },
  }}
/>

export const Template = (args) =>
  html`<saas-table
    data="${Array.isArray(args.data) ? JSON.stringify(args.data) : null}"
  ></saas-table>`;

# Table component

<Canvas>
  <Story name="Default" args={{ data: [{ foo: "bar", hello: "world" }] }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
