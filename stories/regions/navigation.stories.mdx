import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-element";

<Meta
  title="Regions/Navigation"
  argTypes={{
    logo: {
      description: "Logo url",
      table: {
        type: {
          summary: "String",
        },
      },
      control: {
        type: "text",
      },
    },
    left: {
      description: "Left menu",
      table: {
        type: {
          summary: "Array",
        },
      },
      control: {
        type: "array",
      },
    },
    right: {
      description: "Right menu",
      table: {
        type: {
          summary: "Array",
        },
      },
      control: {
        type: "array",
      },
    },
  }}
/>

export const Template = (args) => html` <saas-navbar>
  <img slot="logo" src="https://avatars3.githubusercontent.com/u/2159051?v=4" />
  ${args.left.map(
    (entry) =>
      html`<saas-navbar-item  label="${entry.label}" link="${entry.link}""></saas-navbar-item>`
  )} ${args.right.map(
    (entry) =>
      html`<saas-navbar-item slot="right" label="${entry.label}" link="${entry.link}""></saas-navbar-item>`
  )}
</saas-navbar>`;

# Button component

<Canvas>
  <Story
    name="Default"
    args={{
      logo: "https://avatars3.githubusercontent.com/u/2159051?v=4",
      left: [{ label: "Home", link: "home" }],
      right: [{ label: "Help", link: "help" }],
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />
